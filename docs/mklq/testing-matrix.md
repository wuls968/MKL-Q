# MKL-Q Testing Matrix

This matrix maps the current MKL-Q tests, gates, and benchmark helpers to the
behavior they prove. It is a maintenance guide, not a certification statement.

Use it before changing backend behavior, target configuration, sampling,
measurement/reset, benchmark scripts, or public release metadata.

## Gate Summary

| Gate | Command | Proves | Does Not Prove |
| --- | --- | --- | --- |
| Preflight audit | `python3 benchmarks/mklq/run_preflight_audit.py` | Local Git remotes, shallow state, stale Git lock files, tracked generated/raw artifact policy, `public_report_references` coverage for concrete public docs or workflows `benchmarks/mklq/reports/*.json` references, ignored local artifact summary, and live `main` branch protection settings | Backend correctness, build health, benchmark evidence quality, or latest GitHub Actions success |
| Public hygiene | `.github/workflows/mklq-public-hygiene.yml` or the equivalent local commands in `developer-workflow.md` | Public metadata, public claim-boundary wording, ignored-artifact policy, sanitized benchmark JSON parsing, bounded CPU sampling/probability counter and Metal runtime counter probe JSON parsing including complete expected counter-test coverage, benchmark helper syntax | CUDA-Q build, Apple Silicon runtime behavior beyond tracked evidence, Metal device behavior beyond tracked evidence, release readiness |
| Public release checklist audit | `python3 benchmarks/mklq/run_public_release_checklist_audit.py` | `public-release-checklist.md` contains the required source-only sections, gate commands, referenced docs/scripts, preflight public report-reference boundary, and stop conditions; `developer-workflow.md` keeps the public hygiene command set, counter aggregate-count boundary, and `public_report_references` warning | Builds, benchmarks, GitHub state, backend correctness, or release certification |
| Upstream sync audit | `python3 benchmarks/mklq/run_upstream_sync_audit.py` | Local upstream-sync topology, local `main`/`origin/main`/`upstream/main` refs, optional live upstream freshness, upstream delta counts, manual-review path categories, and `upstream-sync.md` guard coverage | Conflict resolution, build correctness, latest remote freshness unless `--check-remote` is used, or proof that an upstream sync is safe to merge |
| Self-hosted Apple Silicon CI audit | `python3 benchmarks/mklq/run_self_hosted_ci_audit.py` | `apple-silicon-ci.md` defines the self-hosted macOS ARM64 runner requirements, activation checklist, safety boundaries, and full correctness command while confirming the tracked Apple Silicon workflow keeps only a lightweight `main` push guard automatic and keeps the full self-hosted job manual, read-only, source-only, and default-off. With `--check-runners`, it also queries the live GitHub `actions/runners` inventory for an online runner with the required labels before a full-gate dispatch. | Hosted Apple Silicon correctness, build health, backend correctness, or release certification |
| Public health check | `python3 benchmarks/mklq/run_public_healthcheck.py` | Local public hygiene, release checklist coverage, upstream sync audit coverage, public claim-boundary wording, benchmark summary parseability, static clean CPU performance evidence guard, sampling profile evidence guard, static experimental Metal evidence boundary guard, bounded CPU sampling/probability counter and Metal runtime counter probe JSON parsing including `expected == selected` and `missing == 0`, CPU/Metal counter docs drift detection, helper syntax, markdown links, benchmark evidence regeneration consistency, benchmark harness tests, and in `--full` mode the install-prefix build plus local macOS install-prefix signature repair before correctness | CUDA-Q build or backend correctness unless `--full` is used; benchmark refresh unless `--refresh-clean-cpu-benchmark` is used; release artifact signing |
| Public readiness audit | `python3 benchmarks/mklq/run_public_readiness_audit.py` | Pushed public fork identity, clean local `main` parity with `origin/main`, tracked artifact policy, workflow set, issue template set, issue-template label declarations, live GitHub label metadata, live branch protection versus `.github/branch-protection-main.json`, public claim-boundary wording, latest public hygiene workflow success, and no tags or releases | Backend correctness, benchmark refresh, Apple Silicon CI, package readiness, or release certification |
| One-command correctness | `python3 benchmarks/mklq/run_correctness_gate.py --install-prefix "${HOME}/.cudaq-mklq" --build-dir build-python` | Python target smoke, CPU fixtures, Metal fixtures, `nvq++` smoke, selected build-tree `ctest` tests, and the Metal runtime counter probe | Full upstream CUDA-Q test suite, cross-machine performance, packaging, or proof that every `mklq-metal` operation stays on Metal |
| Build-tree Python smoke | `PYTHONPATH="$(pwd)/build-python/python" python3 -m pytest ... -q` | Source-tree Python API behavior against the build products | Installed prefix behavior unless paths are changed |
| Install-prefix Python smoke | `PYTHONPATH="${HOME}/.cudaq-mklq" python3 -m pytest ... -q` | Installed Python package target behavior | C++ `nvq++` behavior unless the nvq++ smoke also runs |
| TargetConfig `ctest` | `ctest --test-dir build-python -R "(mklq_(cpu|metal)_MKLQ|backend_target_setter_check|TargetConfigTester)" --output-on-failure` | MKL-Q backend gtests, target config tests, target setter checks selected by the regex | Every CUDA-Q target test |
| CPU gate counter probe | `python3 benchmarks/mklq/run_cpu_gate_counter_probe.py --build-dir build-python --output benchmarks/mklq/reports/local-cpu-gate-counter-probe-YYYY-MM-DD.cpu-gate-counter.json` | The complete expected build-tree `mklq_cpu_MKLQCpuTester.*` CPU gate fast-path counter-test set is present and each exact ctest asserts native fast-path counters for uncontrolled, controlled, single-control H/Y/Rx/Ry direct pair, single-control Rz phase, composite, multi-control boundary, phase, two-qubit, and three-qubit paths | Timing performance, release sign-off, or cross-machine performance proof |
| CPU gate counter summary | `python3 benchmarks/mklq/summarize_cpu_gate_counters.py --reports benchmarks/mklq/reports --output docs/mklq/cpu-gate-counters.md` | Public Markdown rendering of tracked bounded CPU gate fast-path counter coverage by route category, with aggregate counts explicitly summed across tracked reports | New runtime evidence, timing performance, release sign-off, or cross-machine performance proof |
| CPU gate counter docs guard | `python3 benchmarks/mklq/check_cpu_gate_counter_docs.py` | Tracked public CPU gate counter Markdown matches regenerated output from tracked bounded `.cpu-gate-counter.json` reports and the renderer keeps the aggregate-count note | New runtime evidence, timing performance, release sign-off, or cross-machine performance proof |
| CPU sampling counter probe | `python3 benchmarks/mklq/run_cpu_sampling_counter_probe.py --build-dir build-python --output benchmarks/mklq/reports/local-cpu-sampling-counter-probe-YYYY-MM-DD.cpu-counter.json` | The complete expected build-tree `mklq_cpu_MKLQCpuTester.*` CPU sampling phase and probability-fill counter-test set is present and each exact ctest asserts native sampling/probability counters for sparse full-register scan hit/miss, full-register counts-only, partial-register counts-only, sequential sampling, full-register probability fill, and marginal probability fill routes | Timing performance, release sign-off, or cross-machine performance proof |
| CPU sampling counter summary | `python3 benchmarks/mklq/summarize_cpu_sampling_counters.py --reports benchmarks/mklq/reports --output docs/mklq/cpu-sampling-counters.md` | Public Markdown rendering of tracked bounded CPU sampling phase and probability-fill counter coverage by route category, with aggregate counts explicitly summed across tracked reports | New runtime evidence, timing performance, release sign-off, or cross-machine performance proof |
| CPU sampling counter docs guard | `python3 benchmarks/mklq/check_cpu_sampling_counter_docs.py` | Tracked public CPU sampling counter Markdown matches regenerated output from tracked bounded `.cpu-counter.json` reports and the renderer keeps the aggregate-count note | New runtime evidence, timing performance, release sign-off, or cross-machine performance proof |
| Metal runtime counter probe | `python3 benchmarks/mklq/run_metal_runtime_counter_probe.py --build-dir build-python --output benchmarks/mklq/reports/local-metal-runtime-counter-probe-YYYY-MM-DD.counter.json` | The complete expected build-tree Metal counter-test set is present and each exact `mklq_metal_MKLQMetalTester.*` ctest asserts real `MetalStateVectorExecutor` counters plus MKL-Q simulator test-accessor counters for resident gate, probability, sampling, deterministic sequential/counts-only draw-loop bypasses, host-side sequential/counts-only draw batches, native sampling phase timing accumulators, measurement, collapse, reset, and unsupported-gate fallback/reupload paths | Timing performance, release sign-off, or proof that every `mklq-metal` operation stays on Metal |
| Metal runtime counter summary | `python3 benchmarks/mklq/summarize_metal_runtime_counters.py --reports benchmarks/mklq/reports --output docs/mklq/metal-runtime-counters.md` | Public Markdown rendering of tracked bounded Metal counter coverage by category, with aggregate counts explicitly summed across tracked reports | New runtime evidence, timing performance, release sign-off, or proof that every `mklq-metal` operation stays on Metal |
| Metal runtime counter docs guard | `python3 benchmarks/mklq/check_metal_runtime_counter_docs.py` | Tracked public counter Markdown matches regenerated output from tracked bounded `.counter.json` reports and the renderer keeps the aggregate-count note | New runtime evidence, timing performance, release sign-off, or proof that every `mklq-metal` operation stays on Metal |
| Benchmark dry run | `python3 benchmarks/mklq/bench_mklq_targets.py --dry-run ...` | Benchmark planning, schema, case expansion, row metadata | Runtime performance |
| Clean CPU benchmark | `python3 benchmarks/mklq/run_clean_cpu_benchmark.py --pythonpath "${HOME}/.cudaq-mklq" --stamp YYYY-MM-DD` | Clean-worktree local qpp-cpu vs mklq-cpu benchmark evidence and sanitized report regeneration | Release sign-off or results on another machine |

## Test Artifacts

| Artifact | Scope | Evidence |
| --- | --- | --- |
| `python/tests/backends/test_mklq_python_api.py` | Python API smoke for `mklq-cpu` and `mklq-metal` | `cudaq.set_target`, `cudaq.sample`, `cudaq.get_state`, `cudaq.observe`, observe lists, shots observe, decorator-kernel `cudaq.run`, mid-circuit feedback/reset |
| `python/tests/backends/test_mklq_cpu_correctness_fixtures.py` | CPU circuit correctness fixtures | Bell, GHZ, sampling support, partial-register sampling bit order, qpp-cpu marginal sampling oracle checks, QFT-like fixtures, deterministic and seeded Clifford behavior, phase-family and controlled phase gates, parameterized state/observable comparisons, and hardware-efficient ansatz state/observable comparisons against `qpp-cpu` |
| `python/tests/backends/test_mklq_metal_correctness_fixtures.py` | Experimental Metal mixed-path fixtures | Resident single-target, controlled single-target, two-target, three-target mixed-path fixtures, QFT-like, seeded Clifford, deterministic sampling, partial-register sampling bit order, qpp-cpu marginal sampling oracle checks, resident measurement feedback, resident reset after measurement, qpp-cpu tolerance comparisons |
| `python/tests/backends/test_mklq_nvqpp_smoke.py` | Installed or build-tree `nvq++` smoke | `nvq++ --target mklq-cpu`, `nvq++ --target mklq-metal`, target markers, runtime smoke output |
| `python/tests/builder/test_mklq_targets.py` | Builder-level and target behavior | Target presence, precision tolerance, bit order, sampling boundary cases, OpenMP-sized state comparisons, CPU hot paths, state import/export error handling |
| `python/tests/backends/test_mklq_benchmark_harness.py` | Benchmark and report tooling | Dry-run schemas, case expansion, row isolation, summary generation, sanitized evidence fields, probability microbenchmark schemas |
| `python/tests/mklq_test_utils.py` | Test availability helper | Skips MKL-Q tests when targets are unavailable |
| `targettests/TargetConfig/mklq_targets.config` | Target config file checks | Source and installed YAML existence, backend names, marker defines |
| `targettests/TargetConfig/mklq_runtime_smoke.cpp` | C++ target runtime smoke | Target marker exclusivity, sample/observe/mid-circuit behavior through `nvq++` |
| `unittests/nvqpp/backends/MKLQCpuTester.cpp` | CPU backend implementation tests | Backend edge cases, sampling internals, probability fills, built-in fast paths, custom operation fallback behavior |
| `unittests/nvqpp/backends/MKLQMetalTester.cpp` | Metal runtime and mixed-path simulator tests | Metal device detection, resident single-, two-, and three-target gate kernels, probability fills, marginal paths, requested-order partial-register sampling route counters, deterministic sequential/counts-only draw-loop bypasses, host-side sequential/counts-only draw telemetry and native phase timing after resident marginal probability work, measurement/collapse/reset paths, unsupported-gate fallback/reupload behavior, and fallback/error behavior |
| `benchmarks/mklq/run_correctness_gate.py` | Correctness gate wrapper | Aggregates Python smoke, `nvq++` smoke, selected `ctest`, and ignored Metal runtime counter probe output into local JSON |
| `benchmarks/mklq/check_performance_evidence.py` | Static performance evidence guard | Checks tracked clean CPU sanitized summaries for clean-worktree provenance, ignored raw payload paths, cross-machine disclaimer, required q20 ratios, and positive candidate medians |
| `benchmarks/mklq/check_public_claims.py` | Public claim-boundary guard | Scans public README/docs for required source-only, experimental Metal, non-release, and non-cross-machine wording while rejecting non-negated release-ready, full-Metal-native, or certification claims |
| `benchmarks/mklq/check_sampling_profile_evidence.py` | Sampling profile evidence guard | Checks tracked q20/q22 sampling profile summaries for required full/partial-register rows, positive harness-level timing fields, ignored raw payload boundaries, and explicit non-native-counter wording |
| `benchmarks/mklq/check_metal_evidence.py` | Static Metal evidence guard | Checks tracked `mklq-metal` sanitized summaries for local tuning provenance, ignored raw payload paths, successful Metal rows, and mixed-path/resident/host-readback wording |
| `benchmarks/mklq/run_cpu_gate_counter_probe.py` | CPU gate counter probe | Requires the complete expected CPU gate fast-path counter-test set, including per-gate single-control H/Y/Rx/Ry direct pair paths, single-control Rz direct phase path, and hardware-efficient ansatz composite path selection, runs each selected ctest independently, and writes bounded `.cpu-gate-counter.json` evidence without raw logs or release/performance claims |
| `benchmarks/mklq/summarize_cpu_gate_counters.py` | CPU gate counter summary renderer | Builds `docs/mklq/cpu-gate-counters.md` from bounded `.cpu-gate-counter.json` probe reports; aggregate counts are summed across tracked reports |
| `benchmarks/mklq/check_cpu_gate_counter_docs.py` | CPU gate counter docs guard | Fails when `docs/mklq/cpu-gate-counters.md` differs from the renderer output for tracked bounded CPU gate counter reports or the renderer drops the aggregate-count note |
| `benchmarks/mklq/run_cpu_sampling_counter_probe.py` | CPU sampling counter probe | Requires the complete expected CPU sampling phase and probability-fill counter-test set, including sparse full-register scan hit/miss and explicit full-register/marginal probability-fill cases, runs each selected ctest independently, and writes bounded `.cpu-counter.json` evidence without raw logs or release/performance claims |
| `benchmarks/mklq/summarize_cpu_sampling_counters.py` | CPU sampling counter summary renderer | Builds `docs/mklq/cpu-sampling-counters.md` from bounded `.cpu-counter.json` probe reports; aggregate counts are summed across tracked reports |
| `benchmarks/mklq/check_cpu_sampling_counter_docs.py` | CPU sampling counter docs guard | Fails when `docs/mklq/cpu-sampling-counters.md` differs from the renderer output for tracked bounded CPU sampling counter reports or the renderer drops the aggregate-count note |
| `benchmarks/mklq/run_metal_runtime_counter_probe.py` | Runtime counter probe | Requires the complete expected `mklq_metal_MKLQMetalTester.*` counter-test set, runs each selected ctest independently, and writes bounded `.counter.json` evidence without raw logs or release claims |
| `benchmarks/mklq/run_preflight_audit.py` | Local PR and publication preflight | Checks Git remotes, shallow state, stale Git lock files, tracked generated/raw artifacts, concrete public docs or workflows report references, ignored local artifact summaries, and live public `main` branch protection; `public_report_references` fails when public docs or workflows reference missing or untracked report files |
| `benchmarks/mklq/run_public_release_checklist_audit.py` | Public release checklist audit | Checks that `docs/mklq/public-release-checklist.md` stays aligned with source-only scope, required commands, referenced files, preflight public report-reference boundary, public healthcheck, pushed-readiness gates, and the developer workflow public hygiene command set |
| `benchmarks/mklq/run_upstream_sync_audit.py` | Upstream sync dry-run audit | Checks local upstream-sync refs, optional live upstream freshness, changed-file risk categories, and `docs/mklq/upstream-sync.md` guard coverage without fetching or merging by default |
| `benchmarks/mklq/run_self_hosted_ci_audit.py` | Self-hosted Apple Silicon CI readiness audit | Checks `docs/mklq/apple-silicon-ci.md` and `.github/workflows/mklq-apple-silicon-ci.yml` for runner, security, broad `main` push guard, manual full-gate dispatch, default-off self-hosted activation, read-only permissions, no secret/release paths, validation-command boundaries, and optional live `actions/runners` inventory with `--check-runners` |
| `benchmarks/mklq/summarize_metal_runtime_counters.py` | Runtime counter summary renderer | Builds `docs/mklq/metal-runtime-counters.md` from bounded `.counter.json` probe reports; aggregate counts are summed across tracked reports |
| `benchmarks/mklq/check_metal_runtime_counter_docs.py` | Runtime counter docs guard | Fails when `docs/mklq/metal-runtime-counters.md` differs from the renderer output for tracked bounded counter reports or the renderer drops the aggregate-count note |
| `docs/mklq/metal-execution-boundary.md` | Metal execution boundary | Documents the mixed resident Metal state, synchronization boundary, CPU-oracle fallback, host-side sampling boundary, error boundary, and explicit non-claims for `mklq-metal` |
| `benchmarks/mklq/run_public_healthcheck.py` | Public maintenance health check | Aggregates local public hygiene checks, release checklist audit, upstream sync audit, summary parse, helper compile, markdown link checks, benchmark evidence regeneration comparison, Metal counter docs drift detection, optional build/correctness/benchmark gates |
| `benchmarks/mklq/repair_macos_install_signatures.py` | Local install-prefix signature repair | Refreshes ad-hoc signatures for local macOS install-prefix dylibs, Python extension loadables, and `bin/` Mach-O executables before Python imports or installed `nvq++` smoke executables load runtime libraries; skipped on non-Darwin platforms and writes ignored local JSON |
| `benchmarks/mklq/run_public_readiness_audit.py` | Pushed public repository readiness audit | Checks clean local `main` parity with `origin/main`, public fork metadata, issue templates, live labels, branch protection JSON drift, public claim boundaries, latest hygiene workflow status, and source-only no-release state |
| `examples/mklq/` | Public getting-started examples | Python and C++ Bell/GHZ, parameterized rotation, controlled-phase kickback, and deterministic Clifford-chain examples for `mklq-cpu` and `mklq-metal`; Python examples and `verify_examples.py` are syntax-checked by public healthcheck, and `--full` runs the examples locally |
| `benchmarks/mklq/bench_mklq_targets.py` | Target benchmark harness | Local timing rows for gate/state/sampling cases, multi-control gate-loop rows, QFT-like composite rows, hardware-efficient ansatz composite rows, CRZ distance distribution and per-distance sweep diagnostic rows, seeded Clifford stress rows, target notes, and static `mklq-metal` path-boundary labels |
| `benchmarks/mklq/bench_probability_kernels.py` | Probability microbenchmark | Local dense probability kernel experiment data and schema |
| `benchmarks/mklq/make_summary.py` | Summary sanitizer | Converts raw local benchmark JSON to public summary JSON |
| `benchmarks/mklq/summarize_reports.py` | Public evidence index renderer | Builds `docs/mklq/benchmark-evidence.md` from sanitized summaries |
| `benchmarks/mklq/run_clean_cpu_benchmark.py` | Clean benchmark gate | Refuses dirty clean-evidence runs unless explicitly overridden; regenerates sanitized summaries and public index |

## Capability Coverage

| Capability | Primary Evidence | Required When Changing | Current Gap |
| --- | --- | --- | --- |
| Target registration | `mklq_targets.config`, `test_mklq_targets.py`, `test_mklq_nvqpp_smoke.py` | Target YAML, backend name, preprocessor marker, install layout | Does not prove backend math |
| Python target API | `test_mklq_python_api.py` | `cudaq.set_target`, Python runtime integration, observe/sample/run behavior, and unsupported `noise_model` fail-fast behavior | Limited to selected smoke kernels |
| C++ target API | `mklq_runtime_smoke.cpp`, `test_mklq_nvqpp_smoke.py` | `nvq++ --target`, target marker behavior, C++ runtime smoke | Not a broad C++ API suite |
| CPU state-vector correctness | `test_mklq_cpu_correctness_fixtures.py`, `test_mklq_targets.py`, `MKLQCpuTester.cpp` | CPU gate semantics, sampling, state import/export, error handling, hot paths | No noisy simulation support; full CUDA-Q backend parity is not covered |
| CPU performance-sensitive paths | `MKLQCpuTester.cpp`, `bench_mklq_targets.py`, clean CPU benchmark summaries, `run_cpu_gate_counter_probe.py`, `run_cpu_sampling_counter_probe.py` | Single-qubit, controlled single-qubit, per-gate single-control H/Y/Rx/Ry direct pair paths, single-control Rz phase, selected two-qubit, QFT-like composite, hardware-efficient ansatz composite, CRZ distance distribution and per-distance sweep diagnostics, seeded Clifford, probability, sampling fast paths, selected native gate fast-path counter assertions, and selected native sampling phase plus probability-fill counter assertions | Benchmark evidence is local machine evidence only; CPU gate/sampling/probability counter evidence is not timing evidence |
| Metal resident gate paths | `test_mklq_metal_correctness_fixtures.py`, `MKLQMetalTester.cpp`, `metal-execution-boundary.md` | Resident single-target, controlled and multi-control single-target, parameterized Rx/Ry/Rz rotations, phase-family S/T/Sdg/Tdg gates, two-target, three-target, control gate kernels, and reupload after four-or-more-target unsupported gate fallback | Does not prove full GPU residency for every operation |
| Metal probability/sampling paths | `test_mklq_metal_correctness_fixtures.py`, `MKLQMetalTester.cpp`, `bench_mklq_targets.py`, sanitized summaries, `run_metal_runtime_counter_probe.py`, `metal-execution-boundary.md` | Full-register probability fill, marginal probability, partial-register sampling bit order, qpp-cpu marginal sampling oracle checks, requested-order partial-register sampling marginal-route counters, deterministic sequential/counts-only draw-loop bypasses for one-outcome distributions, host-side sequential/counts-only draw telemetry and native probability-fill/draw/expectation timing accumulators after resident marginal probability work, sampling path labels, static harness path-boundary labels, selected runtime counter assertions, and explicit host-side sampling boundary wording | Stochastic sample draw/count accumulation remains host-side by design for the current boundary; static labels are not runtime counters, and native phase timers are not release benchmark evidence |
| Metal measurement/reset | `test_mklq_metal_correctness_fixtures.py`, `MKLQMetalTester.cpp`, `mklq_runtime_smoke.cpp`, `run_metal_runtime_counter_probe.py`, `metal-execution-boundary.md` | Resident measured-qubit reduction, collapse, reset, mid-circuit behavior, selected runtime counter assertions, and explicit synchronization/error-boundary wording | Fixture coverage is finite and tolerance-based |
| Benchmark tooling | `test_mklq_benchmark_harness.py`, benchmark helper `py_compile`, summary JSON parse, `check_performance_evidence.py`, `check_public_claims.py`, `check_sampling_profile_evidence.py`, `check_metal_evidence.py`, `run_public_release_checklist_audit.py`, `run_upstream_sync_audit.py`, `run_self_hosted_ci_audit.py`, `repair_macos_install_signatures.py`, `run_cpu_gate_counter_probe.py`, `summarize_cpu_gate_counters.py`, `check_cpu_gate_counter_docs.py`, `run_cpu_sampling_counter_probe.py`, `summarize_cpu_sampling_counters.py`, `check_cpu_sampling_counter_docs.py`, `run_metal_runtime_counter_probe.py`, `summarize_metal_runtime_counters.py`, `check_metal_runtime_counter_docs.py` | Benchmark case/schema/report changes, tracked clean CPU evidence quality, public checklist coverage, upstream-sync guard coverage, self-hosted Apple Silicon CI activation-boundary coverage, local install-prefix signature repair coverage, public claim-boundary wording, tracked sampling profile evidence boundaries, tracked experimental Metal evidence boundary quality, bounded runtime counter evidence parsing, public counter-summary rendering, and counter-doc drift detection | Does not prove current runtime speed unless real benchmark rows run; sampling profile evidence is harness-level timing, not native backend internal counter evidence; local ad-hoc signature repair is not release artifact signing |
| Public release hygiene | `.github/workflows/mklq-public-hygiene.yml`, `run_preflight_audit.py`, `run_public_release_checklist_audit.py`, `run_upstream_sync_audit.py`, `run_self_hosted_ci_audit.py`, `repair_macos_install_signatures.py`, `run_public_healthcheck.py`, `run_public_readiness_audit.py`, `check_public_claims.py`, `public-release-checklist.md` | Public metadata, docs, tracked artifact policy, expected Git remotes, local lock/artifact preflight, concrete public report-reference tracking, issue templates, live label taxonomy, live branch protection reference drift, example file presence, local healthcheck composition, local install-prefix signature repair coverage, release checklist coverage, upstream-sync guard coverage, self-hosted Apple Silicon CI activation-boundary coverage, public claim-boundary wording, static clean CPU evidence guard, static Metal evidence boundary guard, bounded CPU sampling/probability and Metal runtime counter evidence parsing, and CPU/Metal counter docs drift detection | Does not build or run Apple Silicon backend tests unless `run_public_healthcheck.py --full --require-clean` is used locally; does not certify packages, releases, or release artifact signatures |

## Minimum Test Selection By Change Type

| Change Type | Minimum Local Evidence |
| --- | --- |
| Docs-only public metadata | `git diff --check`, public hygiene metadata checks, banned-token scan, `check_public_claims.py`; `run_self_hosted_ci_audit.py` when Apple Silicon CI wording changes |
| Public examples | Example source files, `examples/mklq/README.md`, `run_public_healthcheck.py`, Python example `py_compile`, `examples/mklq/verify_examples.py`; local `nvq++` smoke through `verify_examples.py` if C++ example behavior changes |
| Target YAML or target marker | `targettests/TargetConfig/mklq_targets.config`, `test_mklq_targets.py`, `test_mklq_nvqpp_smoke.py` |
| Python target selection | `python/tests/backends/test_mklq_python_api.py`, `python/tests/builder/test_mklq_targets.py` |
| CPU gate semantics | A new or updated CPU fixture plus `test_mklq_cpu_correctness_fixtures.py` and relevant `MKLQCpuTester` case |
| CPU gate fast-path counters | `MKLQCpuTester.cpp`, `run_cpu_gate_counter_probe.py`, `summarize_cpu_gate_counters.py`, `check_cpu_gate_counter_docs.py`, and benchmark dry run when row metadata changes |
| CPU sampling/probability | `MKLQCpuTester.cpp`, `test_mklq_targets.py`, `run_cpu_sampling_counter_probe.py` when native sampling phase counters change, benchmark dry run; clean benchmark evidence only after correctness passes |
| Metal resident gate path | A new or updated `MKLQMetalTester` case plus `test_mklq_metal_correctness_fixtures.py`; refresh `run_metal_runtime_counter_probe.py` evidence and `docs/mklq/metal-runtime-counters.md` if runtime counter assertions change |
| Metal probability/sampling | A new or updated `test_mklq_metal_correctness_fixtures.py` sampling oracle plus relevant `MKLQMetalTester` counter coverage when runtime counters change; refresh `run_metal_runtime_counter_probe.py` evidence and `docs/mklq/metal-runtime-counters.md` if runtime counter assertions change |
| Metal measurement/reset | `MKLQMetalTester.cpp`, `test_mklq_metal_correctness_fixtures.py`, `mklq_runtime_smoke.cpp` if C++ behavior changes; refresh `run_metal_runtime_counter_probe.py` evidence and `docs/mklq/metal-runtime-counters.md` if runtime counter assertions change |
| Benchmark harness or summary | `test_mklq_benchmark_harness.py`, helper `py_compile`, summary JSON parse, `run_public_release_checklist_audit.py` when release checklist wording changes, `run_upstream_sync_audit.py` when upstream-sync wording or risk categories change, `run_self_hosted_ci_audit.py` when Apple Silicon CI policy or workflow boundaries change, `repair_macos_install_signatures.py --dry-run` when local install-prefix signing flow changes, `check_public_claims.py` when public claim-boundary wording changes, `check_performance_evidence.py` when tracked clean CPU evidence changes, `check_sampling_profile_evidence.py` when tracked sampling profile evidence or wording changes, `check_metal_evidence.py` when tracked `mklq-metal` evidence or wording changes, `summarize_cpu_gate_counters.py` and `check_cpu_gate_counter_docs.py` when tracked CPU gate counter evidence changes, `summarize_cpu_sampling_counters.py` and `check_cpu_sampling_counter_docs.py` when tracked CPU sampling counter evidence changes, `summarize_metal_runtime_counters.py` and `check_metal_runtime_counter_docs.py` when tracked Metal counter evidence changes |
| Public release milestone | Full `public-release-checklist.md`, `run_preflight_audit.py --require-clean`, `run_public_release_checklist_audit.py`, `run_public_healthcheck.py --full --require-clean`, `run_public_readiness_audit.py` after pushing, clean benchmark evidence if performance claims changed |
| Release artifact proposal | `docs/mklq/release-policy.md`, public release checklist, one-command correctness gate, packaging-specific fresh-environment smoke tests |
| Upstream CUDA-Q sync | `docs/mklq/upstream-sync.md`, `run_upstream_sync_audit.py`, public hygiene, one-command correctness gate when runtime or target files changed |

## Known Coverage Gaps

- No automatic or branch-protected Apple Silicon backend correctness CI yet;
  only the lightweight push guard is automatic, and the manual self-hosted full
  job is tracked and boundary-audited without claiming runner success here.
- No wheel, PyPI, installer, signing, or release artifact gate.
- No claim of complete upstream CUDA-Q backend parity.
- No noisy simulation support for MKL-Q targets; matching non-empty
  `noise_model` usage is boundary-tested to fail fast.
- No distributed simulation, multi-GPU, or remote QPU validation.
- No guarantee that every operation stays on Metal for `mklq-metal`; unsupported paths may fall
  back to the MKL-Q fp64 CPU oracle.
- No cross-machine performance certification from tracked benchmark summaries.

## Updating This Matrix

Update this file when adding, removing, renaming, or materially changing:

- MKL-Q target configs;
- Python target tests;
- C++ backend gtests;
- TargetConfig lit tests;
- benchmark cases, schemas, or summaries;
- public release or hygiene gates.
- public examples or example commands.

If a backend change creates a new supported behavior, add the test first or in
the same commit as the behavior. If a test only proves metadata or planning,
do not describe it as runtime correctness evidence.
